import 'package:flutter/material.dart';

class DisplayFlightsPage extends StatelessWidget {
  const DisplayFlightsPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(),
    );
  }
}
